[pytest]
addopts = --strict-markers
markers =
    integration: integration tests spanning multiple J-Wash components
    offload: tests requiring Accelerate parameter or disk offload
    gguf: tests exercising GGUF preparation and worker flows
    slow: tests with comparatively long runtime
